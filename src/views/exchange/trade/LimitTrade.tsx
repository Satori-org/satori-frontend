import React from 'react';
import { useTranslation } from 'react-i18next';

export default function LimitTrade() {
    const {t} = useTranslation();

    return (
        <div>

        </div>
    )
}
